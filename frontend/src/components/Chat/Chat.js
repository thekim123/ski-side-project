import React from 'react'
import { MdKeyboardCapslock } from 'react-icons/md';
import styled from 'styled-components'
/*
export default function Chat() {

    const test = () => {
        setInterval(getLastMessages, 2000);

        var apacheClientEndpoint = 'chat';
        var keycloakBaseUrl = 'http://localhost:8081/auth';

        var apacheTomcatServerBaseUrl = "http://localhost:8080/";
        var apacheTomcatWebAppUrlExt = "chatWebApplication/";

        var currentAppUrl = apacheTomcatServerBaseUrl + apacheClientEndpoint;

        var userId;
        var userName;

        var messagesFromDB;
    }

    const initKeycloak = () => {
        var keycloak = new Keycloak({
            url: keycloakBaseUrl,
            realm: 'RealmOne',
            clientId: 'chatApp'
        })

        keycloak.init({
            onLoad: 'login-required'
        }).success(function() {
            generateLoggedInHTML();
        })
    }
    return (
    <div>
        <div id="loggedInPlaceHolder"></div>
        <div id="displayUserInfoPlaceHolder"></div>
        <div id="messageSection"></div>
        <div id="timePlaceHolder"></div>
        <div id="logoutPlaceHolder"></div>
    </div>
    )
}*/
